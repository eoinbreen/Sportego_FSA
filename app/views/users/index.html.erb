<% provide(:title, 'All users') %>


<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.profile_pic.url(:small)%>
      <%= user.username %>
      <button>Add User</button>
    </li>
  <% end %>
</ul>